<%- include('partials/header') %>
<div class="container">
  <div class="centered jumbotron">
    <h1><%=result.name %></h1>
  </div>
  <div>
    <img
      src="<%= result.image %>
    "
      alt="<%= result.name %>"
    />
  </div>

  <caption>
    <p class="captionText"><%= result.description %></p>
  </caption>
</div>
